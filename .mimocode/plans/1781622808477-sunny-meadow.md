# Plan: Fix All Issues in SnapVision Desktop

## Executive Summary
The codebase has **13 issues** across security, bugs, input validation, and code quality. This plan prioritizes critical security fixes and bugs, then addresses code quality improvements.

---

## Priority 1: CRITICAL SECURITY (Must Fix)

### 1.1 Remove Traceback Leaking to Clients
**Files:** `backend/ocr_server.py`
- **Line 564:** Remove `'error_detail': traceback.format_exc()` from `/stock-extract` error response
- **Line 959:** Remove `'detail': traceback.format_exc()` from global 500 handler
- **Action:** Replace with generic error messages; keep logging internally

### 1.2 Add Path Validation for `image_path`
**File:** `backend/ocr_server.py`
- **Lines 315-320:** Add path validation in `/stock-extract` endpoint
- **Action:** Restrict to allowed directories (e.g., `/tmp`, user's home, or validate against Electron's path checks)

### 1.3 Add Input Size Limits
**File:** `backend/ocr_server.py`
- **Lines 289-313:** Add validation for `image_base64` length before processing
- **Action:** Reject payloads > 20MB (Electron limits to ~14MB, add buffer)

### 1.4 Fix Unchecked `int()` Cast
**File:** `backend/ocr_server.py`
- **Line 715:** Wrap in try/except or use `int(data.get('count', 30) or 30)` with validation
- **Action:** Add type check and default fallback

---

## Priority 2: BUGS (Should Fix)

### 2.1 Fix `_adaptive_threshold` Fallback
**File:** `backend/ocr_price.py`
- **Lines 168-180:** Remove dead code (lines 169, 171 assignments to throwaway variables)
- **Action:** Simplify fallback logic, keep only the working numpy path

### 2.2 Fix Chinese Name Detection Logic
**Files:** `backend/stock_parser.py` (line 181), `backend/stock_extractor.py` (line 561)
- **Bug:** `non_chinese = stripped[chinese_chars:]` uses count as index (wrong for mixed strings)
- **Action:** Use proper filtering: `non_chinese = ''.join(c for c in stripped if not is_chinese(c))`

### 2.3 Remove Unused Imports
**File:** `backend/ocr_server.py`
- **Lines 28-29:** Remove `extract_stock_info as extract_stock_info_v2` and `filterStockInfo` (never used)

---

## Priority 3: INPUT VALIDATION (Should Fix)

### 3.1 Add Type Validation for `stock_code`
**File:** `backend/ocr_server.py`
- **Line 644:** Add `isinstance(data['stock_code'], str)` check before `.strip().upper()`

### 3.2 Add Type Validation for `count`
**File:** `backend/ocr_server.py`
- **Line 715:** Validate `count` is int or convertible; default to 30 if invalid

---

## Priority 4: CODE QUALITY (Nice to Have)

### 4.1 Remove Dead Dependency
**File:** `backend/requirements.txt`
- **Line 8:** Remove `requests>=2.28` (unused, server uses `urllib.request`)

### 4.2 Fix Electron Builder Bundle
**File:** `package.json`
- **Lines 64-69:** Exclude test files from `extraResources` by using `filter` pattern

### 4.3 Add `engines` Field
**File:** `package.json`
- Add `"engines": { "node": ">=18" }` to specify minimum Node.js version

---

## Implementation Order

1. **Security fixes** (1.1-1.4) - Highest priority, quick wins
2. **Bug fixes** (2.1-2.3) - Important for correctness
3. **Input validation** (3.1-3.2) - Prevents crashes
4. **Code quality** (4.1-4.3) - Nice to have, optional

---

## Files to Modify

| File | Changes |
|------|---------|
| `backend/ocr_server.py` | Remove traceback leaking, add path validation, input size limits, fix int cast, remove unused imports, add type checks |
| `backend/ocr_price.py` | Fix _adaptive_threshold fallback dead code |
| `backend/stock_parser.py` | Fix _is_likely_cn_name logic |
| `backend/stock_extractor.py` | Fix _is_likely_cn_name logic |
| `backend/requirements.txt` | Remove unused `requests` dependency |
| `package.json` | Add engines field, filter test files from bundle |

---

## Verification

### After Implementation:
1. **Run Python tests:** `cd backend && python -m pytest`
2. **Run TypeScript tests:** `npm run test`
3. **Run build:** `npm run build`
4. **Manual testing:**
   - Test `/stock-extract` with invalid image_path (should reject)
   - Test `/stock-api/kline` with non-numeric count (should return 400, not 500)
   - Verify error responses don't contain tracebacks

---

## Notes

- **Not fixing:** Thread safety (line 39-46), `sandbox: false` (line 1141), code duplication (~400 lines) - these are architectural issues requiring larger refactoring
- **Risk:** Path validation needs careful testing to ensure legitimate paths still work
- **Alternative considered:** Full refactor of stock_parser/stock_extractor duplication - deferred as too large for this fix
